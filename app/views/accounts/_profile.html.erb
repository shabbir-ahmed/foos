    <div id="message">
		<%= render partial: "flash/flash" %>
	</div>
	<!-- sign up-page -->
	<div class="login-page about">
		<%= image_tag "img3.jpg", class: "login-w3img" %>
		<div class="container"> 
			<h3 class="w3ls-title w3ls-title1">Profile</h3>  
			<div class="login-agileinfo"> 
				    
			    <table class="table"> 
				    <thead> 
				    	<tr> 
				    		 <th>Name</th> 
				    		 <th>Email</th> 
				    		 <th>Phone</th> 
				    		 <th>Location</th> 
				    	</tr> 
				    </thead> 
				    <tbody> 
				    	<tr> 
				    		<td style="cursor: pointer;"><%= best_in_place @user, :full_name, as: :input, url: account_path(current_user) %> </i></td> 
				    		<td style="cursor: pointer;"><%= best_in_place @user, :email, as: :input, url: account_path(current_user) %></td> 
				    		<td style="cursor: pointer;"><%= best_in_place @user, :phone, as: :input, url: account_path(current_user) %></td> 
				    		<td style="cursor: pointer;"><%= best_in_place @user, :address, as: :input, url: account_path(current_user) %></td> 
				    	</tr> 
				    </tbody> 
			   	</table>
				
			</div>	 
		</div>
	</div>